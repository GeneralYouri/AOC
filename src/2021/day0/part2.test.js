// const { part2, defaultInput } = require('.');

test('Provided test cases', () => {
    // expect(part2()).toBe(undefined);
});

test('Puzzle input', () => {
    // expect(part2(defaultInput)).toBe(undefined);
});

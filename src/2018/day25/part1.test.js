const { part1, defaultInput } = require('.');

test('Provided test cases', () => {
    expect(part1('')).toBe();
    expect(part1('')).toBe();
    expect(part1('')).toBe();
    expect(part1('')).toBe();
    expect(part1('')).toBe();
});

test('Puzzle input', () => {
    expect(part1(defaultInput)).toBe();
});
